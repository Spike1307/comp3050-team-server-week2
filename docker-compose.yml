services:
  server:
  build: 
    context: ./comp3050-team-server-week2
  expose:
    - "8000"

nginx:
  image: nginx:alpine
  ports:
    - "80:80"
  volumes:
    - ./nginx.conf:/etc/nginx/conf.d/default.conf
  depends_on:
    - server
